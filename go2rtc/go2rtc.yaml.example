# Copy to go2rtc.yaml (auto-created by start_all.sh if missing).
# Streams are upserted at runtime by Gotisheel — do not commit go2rtc.yaml.
api:
  listen: ":1984"

webrtc:
  listen: ":8555"
  candidates:
    - 127.0.0.1
    - stun:stun.l.google.com:19302

rtsp:
  listen: ":8554"

streams: {}
